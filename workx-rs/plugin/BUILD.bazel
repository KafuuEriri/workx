load("//:defs.bzl", "workx_rust_crate")

workx_rust_crate(
    name = "plugin",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ),
    crate_name = "workx_plugin",
)
