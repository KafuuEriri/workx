load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "workx_rust_crate")

workx_rust_crate(
    name = "tui",
    compile_data = glob([
        "assets/**",
        "frames/**",
    ]),
    crate_name = "workx_tui",
    extra_binaries = [
        "//workx-rs/cli:workx",
    ],
    integration_compile_data_extra = ["src/test_backend.rs"],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
        "tests/fixtures/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
