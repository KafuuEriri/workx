load("//:defs.bzl", "workx_rust_crate")

workx_rust_crate(
    name = "exec",
    crate_name = "workx_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
