load("//:defs.bzl", "workx_rust_crate")

workx_rust_crate(
    name = "core-plugins",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ),
    crate_name = "workx_core_plugins",
)
