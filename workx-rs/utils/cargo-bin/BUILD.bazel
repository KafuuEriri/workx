load("//:defs.bzl", "workx_rust_crate")

exports_files(
    ["repo_root.marker"],
    visibility = ["//visibility:public"],
)

workx_rust_crate(
    name = "cargo-bin",
    compile_data = ["repo_root.marker"],
    crate_name = "workx_utils_cargo_bin",
    lib_data_extra = ["repo_root.marker"],
    rustc_env = {
        "WORKX_REPO_ROOT_MARKER": "$(rlocationpath :repo_root.marker)",
    },
    test_data_extra = ["repo_root.marker"],
)
