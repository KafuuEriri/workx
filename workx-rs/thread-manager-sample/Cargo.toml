[package]
name = "workx-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Workx workspace dependency.
# Add new Workx surface area to `workx-core-api` instead of depending on
# additional `workx-*` crates here.
workx-core-api = { workspace = true }
tracing = { workspace = true }
